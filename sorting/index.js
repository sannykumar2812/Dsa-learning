const arrr = [4, 1, 3, 9, 7]


const seflectionSort = (arr, n) => {
    // const n = arr.length
    // let minIndex = 0
    // for (let i = 0; i < n - 1; i++) {

    //     for (let j = i + 1; j < n; j++) {
    //         if (arr[j] < arr[i]) {
    //             minIndex = j
    //             let temp = arr[i]
    //             arr[i] = arr[minIndex]
    //             arr[minIndex] = temp

    //         }
    //         // if (minIndex != i) {
    //         //     [arr[i], arr[minIndex]] = [arr[minIndex], arr[i]]

    //         // }
    //     }
    // }
    // return arr

}

console.log(seflectionSort(arrr, 5))